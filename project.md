# Анализ фронтенд кодовой базы: MyPlanner

## 📁 Структура проекта

### Дерево директорий (src)
```text
src/
├── components/          # UI-компоненты (модульный подход)
│   ├── Calendar/        # Сетка календаря и логика расписания
│   ├── TaskList/        # Группировка и отрисовка списков задач
│   ├── TaskDetail/      # Форма редактирования задачи
│   ├── Sidebar/         # Навигация и управление списками
│   ├── SettingsModal/   # Глобальные настройки приложения
│   └── DatePicker.jsx   # Переиспользуемый выбор даты и времени
├── hooks/               # Бизнес-логика (статус-менеджмент)
│   ├── useTasks.js      # CRUD операций, фильтрация и D&D
│   ├── useAuth.js       # Авторизация (Supabase Auth)
│   └── useUserSettings.js # Синхронизация профиля пользователя
├── assets/              # Статические ресурсы (иконки, изображения)
├── App.jsx              # Главный оркестратор приложения
├── main.jsx             # Точка входа
└── supabase.js          # Конфигурация клиента базы данных
```

### Принципы организации
Код организован по **Layer-based (слоенному)** принципу с четким разделением ответственности:
*   **Слой данных (Supabase)**: Внешний бэкенд.
*   **Слой логики (Hooks)**: Инкапсулирует состояние и взаимодействие с API.
*   **Слой представления (Components)**: Отвечает только за отрисовку и пользовательский ввод.

---

## 🛠 Технологический стек

| Технология | Версия | Назначение |
| :--- | :--- | :--- |
| **React** | 19.2.0 | Основной фронтенд фреймворк |
| **Vite** | 7.2.4 | Инструмент сборки (Bundler) |
| **Supabase** | ^2.89.0 | Backend-as-a-Service (DB, Auth, Realtime) |
| **Tailwind CSS** | ^3.4.17 | Utility-first стилизация |
| **Capacitor** | ^8.0.0 | Кроссплатформенная сборка (Android/iOS) |
| **Google Gemini AI**| ^0.24.1 | Умные подсказки и генерация задач |
| **date-fns** | ^4.1.0 | Работа с датами и временем |

---

## 🏗 Архитектура

### Подходы к компонентной архитектуре
Используется композиция функциональных компонентов. Основные UI-блоки вынесены в изолированные модули, что минимизирует повторный рендеринг `App.jsx`.

### Паттерны разделения логики (Custom Hooks)
Приложение активно использует **Custom Hooks** для извлечения сложной логики из компонентов. 
*Пример использования `useTasks`:*
```javascript
const { 
  tasks, 
  sortedTasks, 
  fetchData, 
  updateTask 
} = useTasks(session);
```

### Управление состоянием
Состояние управляется комбинацией локальных `useState` (для UI) и кастомных хуков, имитирующих работу State Manager. Глобальные данные (сессия, задачи) прокидываются через пропсы, что допустимо для текущего масштаба проекта, но в будущем может потребовать Context API или Zustand.

---

## 🎨 UI/UX и стилизация

*   **Стилизация**: Tailwind CSS обеспечивает высокую скорость разработки и консистентность интерфейса.
*   **Адаптивность**: Используются префиксы Tailwind (`md:`, `lg:`) для поддержки мобильной и десктопной версий (Sidebar превращается в Drawer на мобильных).
*   **Микро-взаимодействия**: Интегрированы библиотеки визуальных эффектов: `canvas-confetti` для завершения задач.
*   **Aesthetics**: Использование градиентов, блюра (`backdrop-blur`) и теней для создания "Glassmorphism" эффектов.

---

## ✅ Качество кода

*   **Линтинг**: Настроен ESLint (`v9.x`) с плагинами для React Hooks и Refresh.
*   **Именование**: Используется `camelCase` для переменных/функций и `PascalCase` для компонентов.
*   **Типизация**: Проект на JavaScript, но настроены типы `@types/react` для поддержки автодополнения в IDE.
*   **Тесты**: На текущий момент автоматизированные тесты отсутствуют.

---

## 🔧 Ключевые компоненты

### 1. `useTasks` (Hub логики)
Центральный хук, управляющий всеми данными.
```javascript
// Инкапсуляция фильтрации
const sortedTasks = tasks
    .filter(t => matchesFilters(t))
    .sort((a, b) => a.order_index - b.order_index);
```

### 2. `CalendarView`
Реализует интерактивную сетку с поддержкой Drag-and-Drop из списка задач прямо в расписание.

### 3. `TaskDetail`
Многофункциональная форма, работающая в двух режимах (Side Panel для десктопа и Modal для мобильных устройств).

---

## 📋 Выводы и рекомендации

### Сильные стороны:
1.  **Чистый `App.jsx`**: После рефакторинга входная точка проекта легко читаема.
2.  **Модульность**: Легко добавлять новые функции без риска сломать существующие.
3.  **Real-time**: Использование Supabase позволяет потенциально добавить совместную работу.

### Рекомендации по улучшению:
1.  **Внедрение Context API**: Для передачи `session` и `theme` во вложенные компоненты (избегание Prop Drilling).
2.  **TypeScript**: Переход на TS повысит надежность работы с данными задач и настройками.
3.  **Error Boundaries**: Добавить более детальную обработку ошибок для AI-запросов и сетевых сбоев.
4.  **Unit Tests**: Начать с тестирования логики фильтрации и группировки в `useTasks`.

---
**Уровень сложности проекта**: Middle-friendly (современный стек, четкая структура, использование продвинутых паттернов React).
